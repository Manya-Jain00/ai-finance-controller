# Runtime dependencies. Install with:  pip install -r requirements.txt
#
# Everything else the project uses (csv, json, dataclasses, argparse, unittest,
# itertools, ...) is in the Python standard library.

google-genai==2.21.0     # the LLM backend — Gemini tool-use loop
python-dotenv==1.2.1      # loads GEMINI_API_KEY from a local .env file
